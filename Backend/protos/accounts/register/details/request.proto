syntax = "proto3";

package accounts.register.details.request;

message Request {
    string username
    string password
    string password_confirmation
    optional string first_name
    optional string last_name
}
