// delete list of summaries from go via their uuid
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func DeleteFromSummaries(c *gin.Context) {
	// Get user uuid
	
	// Delete history item where u.uuid = user_uuid and datatype = summary

	// return
}
