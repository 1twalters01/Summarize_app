// delete list of uuids from history
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func DeleteFromUUIDs(c *gin.Context) {
	// Get user uuid
	// var data_uuids = get uuids

	// Delete history item where u.uuid = user_uuid and data_uuid = data_uuids

	// return
}
