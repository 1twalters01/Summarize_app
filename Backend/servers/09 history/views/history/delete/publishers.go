// delete list of publishers from go via their uuid in the history
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func DeleteFromPublishers(c *gin.Context) {
	// Get user uuid
	
	// Delete history item where u.uuid = user_uuid and datatype = publisher

	// return
}
