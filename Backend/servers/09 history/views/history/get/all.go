// get history
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetHistory(c *gin.Context) {
	// Get user uuid
	// Get history items where user_uuid = user uuid (will need a join)
	// return
}
