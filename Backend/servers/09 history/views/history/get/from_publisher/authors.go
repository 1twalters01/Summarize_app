// Get last read authors from publisher
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func FromPublisherGetAuthors(c *gin.Context) {
	// Get user uuid
    // Get publisher
	// Get history for user with uuid = user uuid and publisher = publisher and type = author
	// return
}
