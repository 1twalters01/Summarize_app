package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func FromPublisherGetSummaries(c *gin.Context) {
	// Get user uuid
    // Get publisher
	// Get history for user with uuid = user uuid and publisher = publisher and type = summary
	// return
}
// Get last read summaries from books by a publisher
