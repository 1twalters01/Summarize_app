package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func FromPublisherGetBooks(c *gin.Context) {
	// Get user uuid
    // Get publisher
	// Get history for user with uuid = user uuid and publisher = publisher and type = book
	// return
}
// Get last read books from publisher
