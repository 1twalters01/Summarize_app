// Get last read summaries from summarizer
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func FromSummarizerGetSummaries(c *gin.Context) {
	// Get user uuid
    // Get summarizer
	// Get history for user with uuid = user uuid and summarizer = summarizer and type = summary
	// return
}

