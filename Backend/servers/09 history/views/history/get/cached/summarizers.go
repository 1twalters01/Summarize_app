// cached last looked at / read summarizers
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetCachedSummarizes(c *gin.Context) {
	// Get user uuid
	// Get All summarizes from cache for user with uuid = user uuid
	// return
}
