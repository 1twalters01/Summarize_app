// cached last read books
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetCachedBooks(c *gin.Context) {
	// Get user uuid
	// Get All books from cache for user with uuid = user uuid
	// return
}
