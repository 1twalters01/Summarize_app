// cached last read authors
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetCachedAuthors(c *gin.Context) {
	// Get user uuid
	// Get All authors from cache for user with uuid = user uuid
	// return
}
