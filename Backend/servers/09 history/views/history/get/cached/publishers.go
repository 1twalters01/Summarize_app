// cached last looked at / read publishers
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetCachedPublishers(c *gin.Context) {
	// Get user uuid
	// Get All publishers from cache for user with uuid = user uuid
	// return
}
