// get list of everything cached in order
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetCachedHistory(c *gin.Context) {
	// Get user uuid
	// Get All history from cache for user with uuid = user uuid
	// return
}
