// cached last read summaries
package views

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetCachedSummaries(c *gin.Context) {
	// Get user uuid
	// Get All summaries from cache for user with uuid = user uuid
	// return
}
