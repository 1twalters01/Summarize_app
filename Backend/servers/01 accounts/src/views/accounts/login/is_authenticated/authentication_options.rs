pub fn get_authentication_options() -> Result<impl Responder> {
    // get user uuid, device id and platform id
    // get totp activation status and otp activation status
    // check if uuid, device and platform have a biometrics activation status else assume false
    // add options to array
    // return array
}